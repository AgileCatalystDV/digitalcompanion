# Create Pull Request

Create a PR for current changes.

1. `git diff` — staged and unstaged changes
2. Write clear commit message
3. Commit and push current branch
4. `gh pr create` — title, description
5. Return PR URL
